import 'package:flutter/material.dart';

class FinanceQuoteScreen extends StatelessWidget {
  const FinanceQuoteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
